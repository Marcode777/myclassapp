
<div class="row">
  <div class="col-md-8">
    <h1>Classes</h1>
    {{#each teacher}}
      <div>
        <h2>{{firstname}} {{lastname}}</h2>
        <ul>
          {{#each students}}
          <li>{{firstname}} {{lastname}}</li>
          {{/each}}
        </ul>
      </div>
    {{/each}}
  </div>